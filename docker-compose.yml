version: '3'

services:
  mysql57srv:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: "mestre"
    ports:
      - "3306:3306"
    volumes:
      - $PWD/data:/var/lib/mysql
      - $PWD/conf:/etc/mysql/mysql.conf.d
    networks:
      - jira-common-network

networks: 
  jira-common-network:
    driver: bridge